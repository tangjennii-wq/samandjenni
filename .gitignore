# raw camera files never belong in the deploy — keep them in ../photos-originals
*.HEIC
*.heic
.DS_Store
img/**/*.HEIC
img/**/*.heic
img/hotels-raw/
img/photos/
img/pictures/

# the engagement shoot — 46 originals, never referenced by the site.
# photos are served from Supabase, not from this public repo.
img/engagement/
